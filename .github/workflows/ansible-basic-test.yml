name: Ansible Playbook Basic Test

on:
  push:
    branches: [main]
    paths:
      - 'infra/**'
  pull_request:
    branches: [main, feature/*, fix/*, refactor/*]
  workflow_dispatch:

env:
  ANSIBLE_HOST_KEY_CHECKING: false
  ANSIBLE_STDOUT_CALLBACK: yaml

jobs:
  test:
    name: Test Playbook
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Ansible
        run: |
          python -m pip install --upgrade pip
          pip install ansible

      - name: Create vault password file
        run: |
          echo "${{ secrets.ANSIBLE_VAULT_PASSWORD }}" > .vault_pass
          chmod 600 .vault_pass
        working-directory: infra

      - name: Run Ansible syntax check
        run: ansible-playbook --syntax-check -i inventory.ini playbook.yml --vault-password-file .vault_pass
        working-directory: infra

      - name: Run playbook in check mode
        run: |
          ansible-playbook -i inventory.ini playbook.yml --check --diff --vault-password-file .vault_pass
        env:
          SSH_AUTH_SOCK: ""
        working-directory: infra
